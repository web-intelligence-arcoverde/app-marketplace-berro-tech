import styled from 'styled-components/native';

import {scale} from '../../../utils';

export const Container = styled.View`
  padding-vertical: ${scale(12)};
  width: ${scale(111)};
  border-right-width: 1;
  border-right-color: #f2f1f7;
  padding-left: ${scale(12)};
`;
