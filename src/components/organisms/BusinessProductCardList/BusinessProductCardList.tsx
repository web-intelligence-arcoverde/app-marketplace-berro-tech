import {ScrollView} from 'react-native';

import {ProductItemCard} from '../../';

export const BusinessProductCardList = () => {
  return (
    <ScrollView>
      <ProductItemCard />
    </ScrollView>
  );
};
