import {KeyboardContainer, HeaderDashboard} from '../../components';

export const BusinessScreen = () => {
  return (
    <KeyboardContainer>
      <HeaderDashboard />
    </KeyboardContainer>
  );
};
