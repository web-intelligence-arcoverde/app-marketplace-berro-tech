import {KeyboardContainer, HeaderDashboard} from '../../components';

export const ProfileScreen = () => {
  return (
    <KeyboardContainer>
      <HeaderDashboard />
    </KeyboardContainer>
  );
};
